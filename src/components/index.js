'use strict';

const users = require('./users/usersRT');
const login = require('./login/loginRT');
const contacts = require('./contacts/contactsRT');

module.exports = {
  users,
  login,
  contacts,
};
